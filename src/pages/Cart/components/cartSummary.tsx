// import { useSelector } from 'react-redux';
import styles from './cartSummary.module.css';
// import { RootState } from '../../../redux/store';

function CartSummary() {
  // const fullBasketData = useSelector((state: RootState) => state.basket.itemsFullFromServer);

  // if (Object.keys(fullBasketData).length > 0) {

  // }

  return <div className={styles.sumMainCont}>SUM MAIN CONT</div>;
}

export default CartSummary;
